<Window x:Class="VVPlayer.ChooseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d" Height="50" Width="360" WindowStyle="None" WindowStartupLocation="CenterOwner"
        WindowState="Normal" ResizeMode="NoResize" ShowInTaskbar="False">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Tag="X" Foreground="Red" Click="ButtonBase_OnClick" HorizontalContentAlignment="Center">关闭</Button>
        <Button Grid.Column="1" Tag="Play" Foreground="Blue" Click="ButtonBase_OnClick" HorizontalContentAlignment="Center">播放</Button>
        <Button Grid.Column="2" Tag="PlayList" Foreground="Green" Click="ButtonBase_OnClick" HorizontalContentAlignment="Center">播放列表</Button>
        <Button Grid.Column="3" Tag="Download" Foreground="Gray" Click="ButtonBase_OnClick" HorizontalContentAlignment="Center">下载</Button>
        <Button Grid.Column="4" Tag="X" Foreground="Red" Click="ButtonBase_OnClick" HorizontalContentAlignment="Center">关闭</Button>
    </Grid>
</Window>